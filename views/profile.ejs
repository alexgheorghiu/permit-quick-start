<!DOCTYPE html>
<head>
    <%- include ('assets') %>
</head>
<html>
	<body>
        <div class="container pt-3">
			<div class="row">
				<div class="col-12">

                <p>
                    ID: <%= user.id %><br/>
                    Utilizator: <%= user.username %><br/>
                    Nume: <%= user.displayName %><br/>
                    <% if (user.emails) { %>
                    Email: <%= user.emails[0].value %><br/>
                    <% } %>
                </p>
                <a href="/logout">Iesire</a>

                </div>
            </div>			
        </div>

	</body>
</html>